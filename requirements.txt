# Sentinel-Agent — çekirdek bağımlılıklar (DESIGN.md §5)
httpx>=0.27,<1.0
pydantic>=2.7,<3.0
PyYAML>=6.0
SQLModel>=0.0.21
playwright>=1.47
PyJWT>=2.8            # JWT exp decode (session expiry)
anthropic>=0.69      # canlı LLM reasoning (Stage 1); lazy import, anahtar gerektirir

# ---- test ----
pytest>=8.2
respx>=0.21          # httpx için network'süz mock
pytest-asyncio>=0.23
